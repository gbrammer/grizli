[build-system]
requires = ["setuptools==67.8.0", "setuptools_scm[toml]>=6.2", "wheel",
            "oldest-supported-numpy", "cython"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
version_file = "grizli/version.py"
